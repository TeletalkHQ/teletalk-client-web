import { errors } from "variables/notifications/errors";
import { successes } from "variables/notifications/successes";

const notifications = {
  error: errors,
  success: successes,
};

export { notifications };
